#pragma once

#include <cstddef>
#include <string_view>

inline constexpr std::string_view kConfigPath = "config.toml";
inline constexpr size_t kFieldCount = 12;
inline constexpr size_t kMaxImsiDigits = 15;
inline constexpr size_t kMaxMsisdnDigits = 15;
